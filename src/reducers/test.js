const initialState = {
    reaction: "loooool",
    nom: "gramos"
}

export default function(state = initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
}